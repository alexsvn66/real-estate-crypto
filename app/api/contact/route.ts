export async function GET() {
  return Response.json({
    ok: true,
    message: "EstateChain contact endpoint placeholder.",
  });
}

export async function POST() {
  return Response.json({
    ok: true,
    message: "EstateChain contact request received.",
  });
}
